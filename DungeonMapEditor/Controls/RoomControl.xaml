<Border x:Class="DungeonMapEditor.Controls.RoomControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:DungeonMapEditor.Controls"
        xmlns:vm="clr-namespace:DungeonMapEditor.ViewModel"
        mc:Ignorable="d" 
        d:DesignHeight="50" d:DesignWidth="50"
        MouseLeftButtonDown="Control_MouseLeftButtonDown" MouseLeftButtonUp="Control_MouseLeftButtonUp"
        MouseMove="Control_MouseMove">
    <Border.DataContext>
        <vm:RoomControlViewModel/>
    </Border.DataContext>
    <Image Source="{Binding RoomAssignment.RoomPlan.RoomPlanImage, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Stretch="UniformToFill" 
           RenderTransformOrigin=".5,.5">
        <Image.RenderTransform>
            <RotateTransform Angle="{Binding RoomAssignment.RoomPlan.Rotation, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
        </Image.RenderTransform>
    </Image>
</Border>
